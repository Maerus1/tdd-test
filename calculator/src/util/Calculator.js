export default class Calculator {
  constructor(){
    
  }

  //basic operations
  add = (x, y) => x + y;

  subtract = (x, y) => x - y;

  multiply = (x, y) => x * y;

  divide = (x, y) => x / y;

}